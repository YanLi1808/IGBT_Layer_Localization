# IGBT_Layer_Localization
Pytorch implementation of Paper "Siamese network for insulated gate bipolar transistor welding layer localization in computed laminography images using sharpness guidance"

![方法流程图](./workflow.png)

Then change the data path `data_dir` in `train.sh` or `test.sh` to train or test.

To train the zero-shot model, you can run:

```bash
sh ./train.sh
